{
  "name": "blackhole",
  "version": "1.0.0",
  "description": "Real-time general-relativistic black hole renderer. Single self-contained `index.html` — no dependencies, no build step, core WebGL2 only.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.28.2",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "puppeteer-core": "^25.8.0"
  }
}
